# VSCode Show Hover in Output

Very simple vscode extension for hover information.

## Usage

Use `showhoverinoutput.showHoverOutput`. It is not bound to anything by default.

![](images/vscode-extension.gif)
